- hosts: garage
  vars_files:
    - secret.yaml
  become: true
  tasks:
    - name: Tailscale
      ansible.builtin.include_role:
        name: tailscale
    - name: Samba
      ansible.builtin.include_role:
        name: smb-shares
    - name: Garage
      ansible.builtin.include_role:
        name: garage
