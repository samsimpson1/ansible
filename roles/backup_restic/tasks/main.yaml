---
- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: backup_restic_state == 'present'

- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: backup_restic_state == 'absent'
