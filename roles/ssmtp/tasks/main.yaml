---
- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: ssmtp_state == 'present'

- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: ssmtp_state == 'absent'
