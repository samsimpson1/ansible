- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: docker_service_state == 'present'
- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: docker_service_state == 'absent'
