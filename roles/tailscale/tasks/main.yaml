---
- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: tailscale_state == 'present'

- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: tailscale_state == 'absent'
