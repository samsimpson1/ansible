---
- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: docker_host_state == 'present'

- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: docker_host_state == 'absent'
