- name: Present tasks
  ansible.builtin.include_tasks:
    file: present.yaml
  when: acme_manager_state == 'present'

- name: Absent tasks
  ansible.builtin.include_tasks:
    file: absent.yaml
  when: acme_manager_state == 'absent'
